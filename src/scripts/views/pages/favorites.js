const Favorites = {
  async render () {
    // eslint-disable-next-line quotes
    return '<h2>Halaman Favorites</h2>'
  },

  async afterRender () {
    // Fungsi ini akan dipanggil setelah render()
  }
}

export default Favorites
